# lws Continuous Security Audit — Findings Index

One row per finding, global ids `F-NNN` assigned in order of filing, never
reused.  Each finding's canonical, self-contained artifact is
`findings/F-NNN.md` (front matter is machine-parseable); this index is the
browse layer, and the originating report in `reports/` is the immutable
pass history.  Severity/confidence definitions in `METHODOLOGY.md`.

Status: `open` | `fixed` (with commit) | `disputed` | `wontfix` (with
reason).  Re-observations of an open finding update `findings/F-NNN.md`,
they do not create a new id.

| Id | Severity | Conf | Unit | Title | Finding | Report | Status |
|---|---|---|---|---|---|---|---|
| F-001 | Medium | high | roles-h1 (flaw in core-net/dummy-callback.c) | CGI stdin NUL-termination writes 1 byte past `pt->serv_buf` end (into `fake_wsi->a.context` LSB) | [F-001](findings/F-001.md) | [SA-001](reports/SA-001-roles-h1.md) | fixed (e59969bd7, pre-rebase twin 40f84eb24) |
| F-002 | Medium | medium | roles-h1 (+ core-net/dummy-callback.c, roles/cgi/ops-cgi.c) | CGI POST body accounting mismatch on partial stdin-pipe writes: truncated CGI body, withheld stdin EOF, leftover bytes reparsed as new request | [F-002](findings/F-002.md) | [SA-001](reports/SA-001-roles-h1.md) | fixed (9d646f7d9; SA-003 verified: EAGAIN→0 backpressure, rx_content_remain re-credit, h2 fails closed) |
| F-003 | Medium | high | roles-cgi | CGI stdout POLLHUP-only wakeup closes stdwsi with undrained payload, truncating CGI responses (race) | [F-003](findings/F-003.md) | [SA-001](reports/SA-001-roles-h1.md) | fixed (e59969bd7) |
| F-004 | Low | high | roles-http | `lws_jwt_sign_token_set_cookie_ascii`: size_t underflow for len < 13 and exact-fit NUL one byte past buf | [F-004](findings/F-004.md) | [SA-002](reports/SA-002-roles-http.md) | fixed (3f1231d4d on main, fix-session twin 3367dede9; underflow confirmed for len<=13, exact-fit claim corrected as not reachable; SA-003 verified fix shape) |
| F-005 | Low | high | roles-http | `lws_http_cookie_get_nth`: documented nonzero-on-overflow contract not implemented (silent truncation); `*max_len == 0` writes NUL into zero-length buffer | [F-005](findings/F-005.md) | [SA-002](reports/SA-002-roles-http.md) | fixed (cf5bc3861; cookie_value_copy() measure-first, returns 2 on overflow, buf untouched; SA-003 verified) |
| F-006 | Low | medium | roles-http | `lws_return_http_status` h1 body append memcpy not bounds-checked vs serv_buf end (F-001 family, config-gated) | [F-006](findings/F-006.md) | [SA-002](reports/SA-002-roles-http.md) | fixed (c19d8fcbe; body staged at end-512, headers bounded by staged body, 512+128 min guard; SA-003 verified) |
| F-007 | Medium | high | roles-http | cookie jar `lws_cookie_write_nsc` leaks `cache_name` on error paths (malformed Expires from untrusted server) — memory-exhaustion DoS | [F-007](findings/F-007.md) | [SA-002](reports/SA-002-roles-http.md) | fixed (99dc57672; SA-003 verified: goto exit cleanup on both paths) |
| F-008 | Low | high | roles-http | cookie jar suffix-domain walk includes numeric hosts — cross-host cookie replay for IP literals (RFC 6265 requires exact match) | [F-008](findings/F-008.md) | [SA-002](reports/SA-002-roles-http.md) | fixed (0616a071e; lws_cookie_is_ip_literal() gate; SA-003 verified) |
| F-009 | Low | high | roles-http | ah lifecheck dump logs peer header values incl. Authorization/Cookie credentials at notice level | [F-009](findings/F-009.md) | [SA-002](reports/SA-002-roles-http.md) | fixed (4f7e2ea9f; SA-004 verified) |
| F-010 | Low | high | roles-h1 (flaw in core-net/dummy-callback.c; dead-gated in roles/cgi/cgi-server.c) | CGI gzip stdin path: partial pipe writes silently dropped, no `post_in_expected` accounting, unconditional full-consume return — latent behind `if (0)` | [F-010](findings/F-010.md) | (F-002 fix session) | fixed (5fa85fe4e; remediation by deletion of the dead gzip stdin machinery — SA-004 verified) |
| F-011 | Low | medium | roles-h2 | `rops_handle_POLLIN_h2` zero-consumed buffered read re-appends the buflist segment instead of retaining it — unbounded buflist growth + duplicated delivery if a parser ever returns 0 on stashed input | [F-011](findings/F-011.md) | (F-002 fix session) | fixed (dc9985646; SA-004 completed the 0-return sweep — no live producer on any path) |
| F-012 | High | high | roles-h2 | hpack dynamic table resize-to-0 frees entries but leaves `used_entries` stale — NULL deref on next indexed header field (unauthenticated remote DoS, default build) | [F-012](findings/F-012.md) | [SA-003](reports/SA-003-roles-h2.md) | fixed (1808dd656 on main, fix-session twin df1e9c70d; 4-part patch incl. the substream-wsi destroy no-op root cause — fix session PoC: crash masked at HEAD, live with partial fix, clean GOAWAY with full fix; 137/137 ctest; SA-005 verified all four parts) |
| F-013 | Low | high | roles-h2 | h2 HEADERS-completion dump logs credential header values at info level (same class as F-009) | [F-013](findings/F-013.md) | [SA-004](reports/SA-004-roles-h2.md) | fixed (3ea888359; F-009 predicate promoted to shared `lws_hdr_token_is_credential()`, three sites redacted incl. two beyond the finding — hpack dyn-table insert log + `_DEBUG` dump_header; runtime-verified zero credential leakage at all log levels, 3-config builds, 130/130 + 30/30 ctest; SA-007 diff-verified) |
| F-014 | Medium | high | roles-ws | pm-deflate client accepts wire-set `tx_buf_size` from the server's extension response — ~1GB per-connection allocation (restrict_args caps RX only; server direction safe) | [F-014](findings/F-014.md) | [SA-006](reports/SA-006-roles-ws.md) | fixed (400c073f7; arg range validation at OPTION_SET/CONFIRM + restrict_args TX cap; api-test-ws-pmd-args red 1073741852-byte alloc / green 4116; 132/132 ctest; SA-008 diff-verified) |
| F-015 | Low | high | roles-h3 | `lws_qpack_decode_encoder_stream` varint decoder lacks the 64-bit shift guard its header-block sibling has — UB on >=10-byte peer varints (downstream consumption bounded) | [F-015](findings/F-015.md) | [SA-008](reports/SA-008-roles-h3.md) | fixed (f60079315; sibling-shaped guard added; UBSan red `shift exponent 70` at qpack.c:1004 pre-fix / zero runtime errors post-fix — un-instrumented x86 -O3 build masks the UB, so UBSan is the load-bearing red; api-test-qpack section 9 + first-ever add_test registration, 131/131 ctest; SA-009 diff-verified) |
| F-016 | Low | high | roles-h3 | h3 qpack header callback logs decoded name=value pairs (credentials incl.) at debug level, and request path (may carry urlarg tokens) at info — F-009/F-013 class, h3 never covered | [F-016](findings/F-016.md) | [SA-008](reports/SA-008-roles-h3.md) | fixed (2b01660eb; shared `lws_hdr_token_is_credential()` redaction in both qpack cb paths + raw `:path` debug dump truncated at '?'; info method/path + adjacent WT notice path logs query-stripped by construction — runtime finding: info site never had the query, `lws_parse_urldecode` splits urlargs first, the live leak was the raw :path debug dump; curl --http3-only red/green: 3 secrets pre-fix / 0 post-fix at all levels, diagnostics preserved; 4-config -Werror builds, 131/131 ctest; SA-009 diff-verified) |

| F-017 | Medium | high | core-net-service | `lws_parse_numeric_address` loop bound admits one extra group — 1-2 byte stack OOB write from malformed 9-group IPv6 / 5-group IPv4 literals; client-reachable via redirect `Location:` targets through the resolver literal paths | [F-017](findings/F-017.md) | [SA-021](reports/SA-021-core-net-network.md) | fixed (43b58d48f; write-time room guards in TOKEN (`+ (ipv6 ? 2 : 1)`, high-byte store moved after check) and back-to-back-`:` DELIMITER (+2) branches, new -15; also covers `::ffff:1.2.3.4.5` mapped-tail 1-byte variant found while fixing; naive `< max_len` loop-bound sketch rejected — breaks valid 8-group literals since ENDED needs one more iteration; api-test-async-dns ipt -15/-12 entries + fixture-free api-test-network sa46/exact-size-4 checks; ASAN red network.c:875 past `a[16]` pre-fix / clean post-fix; 142/142 ctest, 0 warnings; SA-023 diff-verified) |
| F-018 | High | medium | plugin-oauth2 | CRLF response splitting via unencoded urlargs in Location headers (`service_name` pre-auth, `redirect_uri` post-auth) → header injection / session fixation on h1 | [F-018](findings/F-018.md) | [SA-028](reports/SA-028-plugin-oauth2.md) | fixed (715547b25; on main as 09c611026 + the follow-up fences in 3dcbf7b22, verified SA-043; **filed CRLF mechanism disproven by the recheck-by test** — raw-wire probe shows the h1 uri parser truncates urlarg values at a decoded CR and skips the tail, so neither Location sink can emit CR/LF and the `code=` POST-body variant is equally dead; regrade High→Low/Medium recommended. What was live and is now fixed: non-CR/LF C0 controls passed raw into both Location values (red: hop1 `service_name=x%01%02evil` raw, hop2 `got '/\x01\x02evil'`), and `service_name` interpolated unencoded so decoded `& # %` corrupted the authorize query (red: raw `a&b#c%d`). Fix: `urlarg_has_control_bytes()` capture gate (< 0x20, SP allowed — deviation from `< 0x21` sketch, rationale in F-018), redirect_uri→"/" degrade, service_name dropped, `code=` 400 + pending-release, `lws_urlencode()` into the authorize URL, loc 1024→1600; api-test-oauth2-client 4→9 scenarios + set-cookie frag probe + control-byte-free Location checks, red/green captured, 18/18 post-fix. Follow-up session, uncommitted: **both core fences landed** — uri fence (parsers.c refuses any C0/DEL in the URI raw or decoded → 403 h1 / conn-error h2/h3, retiring the CR-truncation quirk and the NUL-in-urlarg contract) and header composition fence (`lws_add_http_header_by_name` name+value and `_by_token` value — own h2/h3 dispatch — reject C0-except-TAB/DEL per RFC 9110 field-content, fail-closed, also fences CRLF in vhost header config); new api-test-http-fence (10 red pre-fence/green), oauth2 scenarios re-pointed at the 403, plugin gates now defense-in-depth; 140/140 ctest all-roles build + 31/31 no-server dimension; docs README.http_parser.md/lws-http.h updated; incoming-request-header-value C0 strictness left as a future-pass observation) |
| F-019 | Medium | high | plugin-oauth2 | Open-redirect bypass: `/\evil.com` passes the `//`-only protocol-relative check, WHATWG URL normalizes backslash → `//evil.com` | [F-019](findings/F-019.md) | [SA-028](reports/SA-028-plugin-oauth2.md) | fixed (7704c0e23; on main as 49b4052e5, verified SA-043; `redirect_uri_has_unsafe_path_bytes()` RFC 3986 path/query whitelist wired into the `//` check block, reset-to-`/`; documented deviations: `>= 0x80` UTF-8 and `#` fragment pass; api-test-oauth2-client 9→12 scenarios red/green — filed `/\evil.com` payload reproduced `Location: /\evil.com` pre-fix, `/` post-fix, UTF-8+fragment positive control round-trips byte-exact; 20/20 steps, 140/140 ctest, -Werror clean; browser-side recheck-by stands for deployment QA) |
| F-020 | Medium | medium | plugin-auth-login | Login CSRF on `/.lws-login-sso`: origin/referer check skipped entirely when both headers absent (sandboxed iframe + no-referrer) | [F-020](findings/F-020.md) | [SA-028](reports/SA-028-plugin-oauth2.md) | fixed (971701804; on main as 6b978df45, verified SA-043; gate fails closed on every non-positive path — headers absent, unparseable Origin/Referer (second fail-open found while fixing), mismatch, or no auth-server-url all 403 with nothing planted; sketch's optional state-nonce binding declined, noted as future hardening; api-test-lws-login-bff 3→8 scenarios with genuinely-signed tokens minted from the plugin's own JWK — red: sso-nohdrs planted `auth_session=<submitted token>` pre-fix, green: both attack variants 403, origin/referer positive controls still plant verbatim; 8/8 scenarios, 165/165 ctest, -Werror clean; SSO docs updated) |
| F-021 | Low | high | plugin-oauth2 (flaw consumed by plugin-auth-login widget JS) | Unauth device registration `name` reaches admin widget DOM via innerHTML — JSON-purified but not HTML-escaped; CSP-gated XSS surface | [F-021](findings/F-021.md) | [SA-028](reports/SA-028-plugin-oauth2.md) | fixed (273c57d63, 2026-08-24 fix session; on main as fadfcc1e1, verified SA-043; render-boundary fix per the sketch — canned widget JS gains `window.lwsLoginEsc()` and every dynamic string composed into innerHTML now goes through it (preauth `d.name`, `st.identity`, `auth_server_url`, computed logout/login URLs), `d.user_code` composed as `encodeURIComponent()` in the Authorize href so it can neither break out of the attribute nor reshape the query; optional server-side printable-charset gate on preauth `name` declined with rationale in F-021 (markup chars are printable, control bytes already inert, would break UTF-8 names); `login-assets/login.js` re-checked, already textContent; api-test-lws-login-bff 8→9 scenarios — new `widget-js-fence` GETs the actually-served /lws-login.js and asserts escaper presence + escaped sinks + absence of the raw `'+d.name+'`/`'+st.identity+'`/`'+d.user_code+'` interpolations, red: all 7 assertions failed pre-fix, green: 9/9; 165/165 ctest, -Werror clean; widget docs updated; browser-side recheck-by reduced to deployment QA) |
| F-022 | Low | medium | plugin-oauth2 | Set-Cookie composition relies on lws_snprintf truncation — trailing SameSite/Secure/HttpOnly silently dropped near buffer cap (class of the login plugin's fixed 96-byte csrf bug) | [F-022](findings/F-022.md) | [SA-028](reports/SA-028-plugin-oauth2.md) | fixed (bb8049582; on main as 0300e5ac0, verified SA-043; class removed via new fail-closed public `lws_http_cookie_compose()` (lws-http.h/parsers.c) wired into every mint/clear site of **both** audited plugins + worst-case buffer sizings + 1..63 cookie-name init gates; oauth2 session-cookie fail-closes the login, sidecars skip-with-err; login sso-path 2048-byte JWT-validate scratch grown to LWS_SSO_MAX_COOKIE (large-but-valid tokens silently 403'd pre-fix); **same class left in never-audited plugin-auth-server — convert at its first audit**; api-test-http-cookie-compose new (fence at the one-byte fit boundary), api-test-oauth2-client 12→13 scenarios red/green (refresh frag lost `Secure; HttpOnly` into the 256-byte buffer pre-fix with 107-byte domain + 120-char refresh token), api-test-lws-login-bff mints ~4059-byte genuinely-signed token (red 403 pre-fix / green 302+full tail) + per-frag tail fences in both e2e tests; 143/143 ctest full build, 32/32 no-server, -Werror clean) |
| F-023 | Low | high | roles-http | lwsws JSON config bump arena (fixed 64KB, lwsws/main.c:70) has no bounds check on any allocation path — struct allocs advance `a->p` unchecked, `dostring` NUL is unconditional, and once `p > end` the `lws_ptr_diff_size_t(a->end, a->p)` size is a huge size_t so vsnprintf writes past the allocation; config author = root (no privilege boundary) — robustness | [F-023](findings/F-023.md) | [SA-042](reports/SA-042-roles-http-finish.md) | fixed (fb34db56e, verified SA-045; bounds-checked arena allocator + loud parse failure, api-test-lejp-conf; SA-042) |
| F-024 | Medium | high | roles-http | fops-zip decompress-mode seek resync resets `exp_uncomp_pos = 0` then calls itself, re-entering the same branch forever — stack exhaustion on any nonzero fop-fd seek; unauth remote via `Range: bytes=1-` (server.c:3706) or partial-write adjust (server.c:3948, no Range needed); `LWS_WITH_ZIP_FOPS` off by default | [F-024](findings/F-024.md) | [SA-042](reports/SA-042-roles-http-finish.md) | fixed (19aa8e577, verified SA-045; inflate loop extracted to bounded `lws_fops_zip_inflate()` + non-recursive progress-guarded discard via caller's buffer + `[0,len]`-clamped seek_cur; also fixed the virtual/physical `cur` mixup and truncated-entry Z_BUF_ERROR spin found in the old loop; **F-025 retired in the same change** per its own recommendation; HEAD cd0f9fddc dll2-sweep misfire in the ZIP_FOPS block of lws_create_context (context.c:1052) made the dimension unbuildable and is repaired en route; new api-test-fops-zip composes its fixture zip in C (stored-deflate block) and red/green: SIGSEGV exit 139 at first seek+read pre-fix / 37-37 checks post-fix; valgrind clean, 135/135 ctest, -Werror clean both dimensions; **SA-045 note: the api-test-fops-zip dir is still untracked — needs git add before it runs in CI**) |
| F-025 | Low | high | roles-http | fops-zip `seek_cur` moves only the virtual pos — STORE and gzip-container modes read from the stale physical zip position after a seek, so ranged requests serve misaligned bytes from elsewhere in the same zip (data integrity; no disclosure beyond the zip) | [F-025](findings/F-025.md) | [SA-042](reports/SA-042-roles-http-finish.md) | fixed (19aa8e577, verified SA-045; seek_cur-translate variant — `zip_fop_fd` repositioned to `content_start + clamp(payload_ofs, 0, eff_size)` incl. the 10-byte container-header offset, with `[0,len]` clamping of the virtual pos; same change/api-test as F-024, misaligned-slice checks red pre-fix / green post-fix) |
| F-026 | Low | high | jose | `lws_jwk_export` delimiter writes (`*p++ = ','`/`']'`) are unbounded past `end`, and truncation is silent — heap OOB + truncated-key-as-success once a caller buffer overflows; untrusted `kid`/`alg`/`x5c` meta are size-uncapped at import; in-tree callers export own keys, the untrusted-key exporters (auth-dns, dht-dnssec) unaudited; `LWS_WITH_JOSE` off by default | [F-026](findings/F-026.md) | [SA-044](reports/SA-044-jose-jwk-jws.md) | fixed (245e7b15f; export rewritten on room-checked `_jwk_ex_printf`/`_putc`/`_putn` helpers — loud -1 with in-bounds NUL on any shortfall, `*len<3` guard, b64 `-4` reserve can no longer wrap; genec's NUL-in-`crv`-`.len` quirk preserved via first-NUL stop (caught red by the ECDH-ES tests); meta import caps kty 8/kid 256/use 8/key_ops 128/x5c 8192/alg 32; finding's ~10.9KB `jps->b64` figure corrected to 1366 bytes and the OOB amplified: wrapped `end-p` made vsnprintf write whole members OOB, ASAN-proven `WRITE of size 12` pre-fix; api-test-jose red/green fence over every buffer length + oversize-kid import rejection; 141/141 ctest JOSE build, ASAN + valgrind clean, exported bytes identical for canned keys; pre-existing ~5KB decrypt-path leak in `lws_jwe_auth_and_decrypt_ecdh` noted for the next jose pass) |
| F-027 | Medium | high | jose | `lws_jwt_signed_validate` lacks the `!jose.alg` guard its sibling `lws_jws_sig_confirm` has (jws.c:474) and derefs `jose.alg->alg` at jws.c:1142 — parse succeeds with alg absent, so a 3-block token with header `{"typ":"JWT"}` NULL-derefs; unauth remote DoS at 5 cookie/token input points (login SSO, http-layer, jwt-auth, deaddrop, dht-monitor); `LWS_WITH_JOSE` off by default but it is the SSO prerequisite | [F-027](findings/F-027.md) | [SA-044](reports/SA-044-jose-jwk-jws.md) | fixed (0b294cfcf, verified SA-045; `|| !jose.alg` folded into the parse checks of `lws_jwt_signed_validate` and — same missing-guard pattern found by the recommended grep — `lws_jwt_vsign_via_info`'s caller-supplied `jose_hdr` path (NULL reached `lws_jws_sign_from_b64`, an exported deref site); auth-dns consumer provably safe (injects `alg` into the header it builds), jwe side already guarded; api-test-jose red/green: `eyJ0eXAiOiJKV1QifQ.e30.AQ` segfault 139 → clean reject 1; login-bff e2e scenario `alg-less-session-jwt` (GET / with alg-less `auth_session` cookie through the per-request gate) server-death 139 → 303 + 10/10 scenarios; 141/141 JOSE ctest, 144/144 plugins+bff ctest, ASAN clean apart from the pre-existing F-026-noted jwe decrypt leak) |
| F-028 | Low | high | jose | `lws_jws_compact_decode_map` reuses loop counter `n` as the b64 decode return — exits after block 0 for any real token, leaving map blocks 1-3 uninitialized stack that `lws_jws_sig_confirm_compact_b64_map` (unchecked 0 return) feeds into sig_confirm as garbage pointers; no in-tree callers — dead public API, fix or delete | [F-028](findings/F-028.md) | [SA-044](reports/SA-044-jose-jwk-jws.md) | fixed (919b2add5, verified SA-046; loop-counter/decode-result split + entry memset + real block-count return so the wrapper gate works, header doc comment; api-test-jose test_jws_b64_map — red SIGSEGV exit 139 / ASAN SIGILL at first confirm pre-fix, green 4-way checks post-fix; 141/141 ctest, ASAN clean) |
| F-029 | Medium | high | jose | ECDH-ES JWE decrypt KW branch checks only a lower bound on `encrypted_key` then `lws_genaes_crypt`s the attacker-length EKEY into `shared_secret[512]` — the openssl backend's crypt is a bare `EVP_DecryptUpdate` with no output bound (KW unwrap emits len-8 bytes) — **pre-auth** stack overflow; the sibling aeskw.c path rejects `EKEY > sizeof(enc_cek)` before the identical call; consumer-gated (no in-tree untrusted JWE consumer), `LWS_WITH_JOSE` off by default | [F-029](findings/F-029.md) | [SA-046](reports/SA-046-jose-jwe.md) | fixed (68c347344a gnutls + 63672c994 openssl/openhitls/schannel parity, verified SA-052; exact-length gate `EKEY == enc_hlen + RFC3394 8` replacing the lower-bound-only check — tighter than the sibling's `>` bound and correct RFC7516 validation, no interop change since conforming producers emit exactly that; api-test-jose `test_ecdhes_ekey_oversize` splices a 576-byte attacker EKEY into a genuine ECDH-ES+A128KW compact; ASAN red `WRITE of size 568` into `shared_secret[512]` at jwe-ecdh-es-aeskw.c:571 via gnutls `lws_genaes_rfc3394_wrap` — backend-independence incidentally confirmed, SA-046 had verified the openssl path by reading / green full api-test-jose under ASAN+LSan exit 0; en route: gnutls success-path pub-slot leak filed+fixed as F-036, bail-path leak stays open as F-030; `lws_genaes_crypt` output-bound API hardening recommendation left open — all KW-decrypt callers now bounded) |
| F-030 | Low | high | jose | `lws_jwe_auth_and_decrypt_ecdh` runs `lws_genec_destroy` on the success path only — the three bail sites after `genecdh_create` leak the backend EC context (~5KB/attempt under openssl ASAN); root cause of the F-026 fix session's "jwe decrypt-path leak" lead; malformed-epk JWEs exhaust memory on decrypting apps | [F-030](findings/F-030.md) | [SA-046](reports/SA-046-jose-jwe.md) | fixed (811e309ca on main, pre-rebase twin 7f697344a, verified SA-051, F-030 fix session; `ecctx` zeroed at declaration + `lws_genec_destroy` moved to `bail:` — runs exactly once on every path past create; enabling that required backend slot hygiene for dangling handles failure paths leave behind, else bail-destroy would double-deinit: gnutls NULLs `ctx->priv`/`ctx->pub` after every failure-path deinit (set_key both branches + new_keypair bail), schannel NULLs the key handle after its pre-import `BCryptDestroyKey`; openssl/mbedtls/bearssl/openhitls surveyed, already safe; api-test-jose `test_ecdhes_malformed_epk` — two malformed-epk variants (short x, off-curve point) ×8 decrypts, red pre-fix under gnutls ASAN+LSan 68768B/571 allocations, green + 0 leaks post-fix on gnutls, openssl and mbedtls 3.6.5 ASAN builds incl. gencrypto/x509/jwt-cookie; bearssl `pub.q` clobber-on-reset noted for a future finding) |
| F-031 | Low | high | jose | jwe create-side derefs without guards: `lws_jwe_encrypt` (jwe.c:416) `jose.alg` after a parse-only check, `lws_jwe_render_flattened` (jwe.c:758) `jose.enc_alg` which nothing validates (lws_jose_render checks alg only) — F-027-class app-input NULL-deref leftovers | [F-031](findings/F-031.md) | [SA-046](reports/SA-046-jose-jwe.md) | fixed (5fadbda2e on main, verified SA-051; both guards added — encrypt mirrors the auth_and_decrypt guard incl. header snippet in the log, render checks enc_alg after lws_jose_render; deviation from sketch: encrypt uses `return -1` not `goto bail` since the bail memset would orphan a parsed epk's heap elements (F-030 leak class), caller's lws_jwe_destroy frees them; recommended grep sweep done — no other unguarded `alg`/`enc_alg` consumers in lib/jose or examples; api-test-jose `test_jwe_create_side_guards` 3 legs red 139/139 pre-fix at both sites incl. epk-without-alg leak leg, green + ASAN+LSan 0 leaks post-fix; 141/141 ctest, -Werror clean; pre-existing parse-fail bail memset epk leak noted for a future pass) |
| F-032 | Low | high | cose | five dead `!alg` guards after container_of(head) — **DISPUTED/closed (SA-063)**: `list` is the FIRST member of `lws_cose_sig_alg_t` (offset 0) and cps is zalloc'd, so an empty list yields exactly NULL and the guards were live; crash mechanism unsound; maintainer confirmed the v5.0-stable helper removal (3586ea385) was deliberate Coverity cleanup of correct code; main-side `alg_get_head()` (f1658e901) is cosmetic only; class-wide sweep found no real instance | [F-032](findings/F-032.md) | [SA-049](reports/SA-049-cose.md) | disputed (false positive on the crash mechanism; SA-061 "regression" retracted — stable tree is correct without the helper) |
| F-033 | Low | high | cose | EdDSA COSE broken both sides: `lws_cose_key_checks(ck, OKP, alg, op, NULL)` (cose_validate_alg.c:143 + cose_sign_alg.c:144) trips key_checks' own `assert(... \|\| crv)` (abort) or `strlen(NULL)` under NDEBUG — attacker-declared EdDSA alg crashes validation pre-signature; api-test only generates EdDSA keys, never signs/validates | [F-033](findings/F-033.md) | [SA-049](reports/SA-049-cose.md) | fixed (73b28a53d on main, verified SA-051; crash reproduced first via the new api-test legs — SIGABRT at cose_key.c:157 `assert((kty != OKP && kty != EC2) \|\| crv)` signing with a freshly generated Ed25519 key. Fixed per the finding's shape: for OKP the curve is per-key, so key_checks now requires the key's own CRV element and accepts it by length+memcmp when it is Ed25519/Ed448 (mirroring exactly what `lws_geneddsa_set_key` takes, incl. the counted-NUL len tolerance) — no assert trip, no strlen(NULL); EC2 keeps the imposed-curve compare and the assert, RSA/symmetric skip the curve block. En route, three more EdDSA-blockers fixed in cose_key.c: cose_curves[] spelled the OKP names "ED25519"/"ED448" so int-crv imports produced elements the crypto backend's case-sensitive match rejects and exports fell back to nonstandard tstr crv — now RFC8037 "Ed25519"/"Ed448"; key_ops array import was doubly broken (`ctx->pst[ctx->sp]` should be `ctx->pst[ctx->pst_sp]`, and cose_state reset after the first array item rejected the rest) — filed as F-038; and "EdDSA" was missing from alg_names so `lws_cose_name_to_alg()`/result printing couldn't resolve it. Coverage: api-test-cose gains `test_cose_sign_eddsa` (sign1 → validate → tamper-fails, for a generated key and for imported int-crv cose_key10) plus a generated-OKP-key export→import round-trip fence in keys.c; red (abort) pre-fix, green post-fix on openssl ASAN+UBSAN+LSan, full api-test-cose PASS on gnutls (EdDSA legs skip via the backend probe), -Werror clean. openssl-backend verification of this test was capped at the EdDSA/keys legs by F-039 until the F-039 fix session lifted the cap — full api-test-cose incl ECDSA legs now green on true openssl 3.5.7) |
| F-034 | Low | high | cose | `lws_cose_key_import` — `cps.ck->gencrypto_kty` NULL-deref on CBOR that parses but never opens a map (eg `[]`), and the partial key leaks when lecp fails without the callback bailing (malformed CBOR) | [F-034](findings/F-034.md) | [SA-049](reports/SA-049-cose.md) | fixed (e4f45266f on main, verified SA-052; exactly the recommended shape — `m < 0` path now destroys `cps.ck` unconditionally before the set destroy (no-op after callback bail, which already NULLed it; the only cleanup for parser-level failures incl. truncation since LECP_CONTINUE is -1), and a new `!cps.ck` gate before the `gencrypto_kty` switch returns NULL with the set destroyed. api-test-cose keys.c gains a degenerate-import section: empty input / `[]` / `null` / `true` / bare int / empty bstr / `{}` / `tag(101) null` / truncated bare tag all must return NULL, the key-set `[]` form too, plus a truncated `cose_key3` and a mid-key `0x5d` reserved-ai corruption for the leak legs. Red pre-fix: UBSAN SIGILL exit 132 on the NULL member access at the first leg; leak red isolated by disabling only the `m < 0` cleanup — LSan shows the two 336B cb_cose_key OBJECT_START leaks via the truncated/malformed legs. Green post-fix: full api-test-cose PASS exit 0 (gnutls, ASAN+UBSAN+LSan), 22/22 cose ctests, -Werror clean; no valid-input behavior change, callers already treat NULL as the documented failure) |
| F-035 | Low | high | cose | CBOR text-string `crv` (and meta[ALG]) stored by hand without NUL — `lws_cose_key_export`'s strcmp-based curve lookup reads past the allocation (OOB read; the `%.*s` fallback and key_checks memcmp are precision/len-bounded) | [F-035](findings/F-035.md) | [SA-049](reports/SA-049-cose.md) | fixed (2e96e85e7 on main, verified SA-052; exactly the recommended shape — both hand-rolled `LECPCB_VAL_STR_END` stores (crv text-string + `meta[ALG]`) routed through `lws_ck_set_el()` so they get the len+1 alloc + NUL every other element store has, bailing on alloc failure as before; `len` still excludes the NUL so key_checks' memcmp paths and the OKP counted-NUL tolerance are unchanged. api-test-cose keys.c gains `cose_key1_tstrcrv` (key1's x/y/d with `{3:"ES256", -1:"P-256"}` as text strings): import asserts the crv and alg elements stored len-exact **and** NUL-terminated (`buf[len] == 0`) with `cose_curve`/`cose_alg` resolved, export asserts FINISHED with the curve emitted as the COSE int pair (`0x20 0x01`, not the tstr fallback the OOB-missed strcmp produces), and the exported form re-imports to identical key material. Red pre-fix: ASAN heap-buffer-overflow READ 1, 0 bytes after the 5-byte crv allocation from `cb_cose_key` cose_key.c:734, exit 1 at the first new leg. Green post-fix: full api-test-cose PASS exit 0 (openssl backend, gcc ASAN), -Werror clean; 17 non-cose ctest fails in this ASAN config diff-verified identical at pristine HEAD) |
| F-036 | Low | high | tls-gnutls (consumer: jose) | gnutls genec `lws_genecdh_set_key` re-inits `ctx->pub` over the existing handle without deinit — 536B + nettle/gmp children leak on **every** two-sided ECDH (all successful ECDH-ES JWE decrypts; gnutls is the in-tree default with HTTP3 on); distinct from F-030's bail paths, this is the success path | [F-036](findings/F-036.md) | (F-029 fix session) | fixed (68c347ac0 gnutls + 63672c994 openssl/openhitls/schannel parity, verified SA-052; `if (ctx->pub) gnutls_pubkey_deinit(ctx->pub)` before both pub-init sites, last-set-wins semantics kept — derive uses priv + last pub only; discovered by LSan once the F-029 fix let api-test-jose survive to exit, previously masked by the overflow abort; api-test-jose + api-test-gencrypto ASAN+LSan clean, 0 leaks on exercised paths; **class completed across backends** in the F-036 completion session, partner-committed `63672c994` — gnutls priv-slot guards (set_key OURS + new_keypair, has_private kept coherent), bearssl pub.q re-set free + new_keypair bail free, openssl/openhitls side-aware slot-releasing imports with success-only OURS-side has_private (+ EdDSA set_key same), mbedtls-v4 peer_key/key_id guards, schannel destroy NULLs handles, has_private cleared in every destroy; api-test-gencrypto `test_genec2` fence (OURS+THEIRS re-set, last-set-wins proven by deriving from re-set ctx vs fresh ctx and comparing) red/green-proven — 4646B/40alloc LSan red with one guard disabled, exit 0 restored; gencrypto/jose/cose ctest-clean zero-leak on gnutls 3.8.9 + openssl 3.5.7 + mbedtls 3.6.5 ASAN, -Werror clean; bearssl/openhitls/schannel/mbedtls-v4 changes are code-read only (not buildable on this box) and mirror each file's existing destroy/guarded-generator idioms; en route filed **F-040** + **F-041** (both open); also noted F-039's openssl ECDSA verify breakage did not reproduce in this session's green openssl jose/cose runs — **corrected by the F-039 fix session: those "openssl" binaries were gnutls-linked** (HTTP3 default forces `LWS_WITH_GNUTLS=ON`; openssl needs `-DLWS_WITH_HTTP3=0 -DLWS_WITH_GNUTLS=OFF`), F-039 reproduces and is now fixed on the true openssl backend) |
| F-037 | Low | high | tls-core | non-TLS-on-TLS-port detection path (`ALLOW_NON_SSL_ON_SSL_PORT`, default-fail branch) hexdumps up to 256 bytes of the peer's first data at **notice** level — a misdirected plaintext request line (path, urlarg tokens, auth-in-URL) lands in logs; F-009/F-013/F-016 class | [F-037](findings/F-037.md) | [SA-050](reports/SA-050-tls-core-1.md) | wontfix (2026-08-28 partner decision: the hexdumped bytes were sent **unencrypted** to a TLS port — already visible to every on-path observer and to the host operator regardless, so the server logging its own received plaintext to the operator's log is no disclosure and breaks no confidentiality expectation; branch is the ≤256B diagnostic payload of the opt-in default-fail "not a valid tls hello" case; distinct from the F-009-family fixes, which logged peer data **after decryption** where a real confidentiality expectation existed — those stay fixed; deployment note: widening the log audience is a log-routing choice, remediation shape stays on the shelf) |
| F-038 | Low | high | cose | `lws_cose_key_import` rejects every COSE key carrying a `key_ops` array (standard in interop keys): the uint handler indexes the *parse* stack with the *value* stack pointer (`ctx->pst[ctx->sp]` for the ppos check, both single-key and key-set branches) so the path/ppos fence never matches, and cose_state is reset after the first array item so the second item hits "uint not allowed in state 0" once the indexing is corrected | [F-038](findings/F-038.md) | (F-033 fix session) | fixed (uncommitted F-033 fix session; found by the new OKP export→import round-trip fence, which exports generated keys **with** their key_ops — `ctx->pst[ctx->pst_sp]` at both checks and keep `cose_state == LWSCOSE_WKK_KEY_OPS` across further items in its array; import of `{...,4:[1,2]}` verified, round-trip fence green) |
| F-039 | Medium | high | tls-openssl (consumers: cose, jose) | openssl backend ECDSA **verify** broken against openssl 3.5.7: in `lws_genecdsa_hash_sig_verify_jws`'s compiled `LWS_HAVE_EVP_PKEY_GET_BN_PARAM` branch, `EVP_PKEY_verify()` itself returns -1 ("missing parameters"; the shared `ECDSA_do_verify fail` log label is a misnomer) — instrumented: DER sig fine (71B), ctx new/init fine, the verify call errors; signature md is never set on the ctx. api-test-cose sign1/sign-pass-01 and api-test-jose ECDSA legs fail on unmodified sources at f1658e901, so COSE/JWS ECDSA validation is untestable on this openssl; gnutls passes the same legs, EdDSA (raw-key EVP path) unaffected | [F-039](findings/F-039.md) | (F-033 fix session) | fixed (d1f752b41 on main, verified SA-052; root cause is **not** the md theory — setting `EVP_PKEY_CTX_set_signature_md` changes nothing and the untouched sign/verify sequence works once the key imports correctly. Both real defects sit in 4e59faf9d's `EVP_PKEY_fromdata()` conversion and fail silently: **(1)** `OSSL_PARAM_construct_BN()` was fed raw big-endian MPIs, but BIGNUM OSSL_PARAMS must be in the provider's native word layout (`BN_bn2nativepad`/`OSSL_PARAM_BLD_push_BN`; proven by `EVP_PKEY_eq(kgen, imported)==0` scrambled vs `==1` native-padded) so every imported BIGNUM is word-scrambled on LE; **(2)** EC keymgmt import has never (3.0→3.5) accepted `qx`/`qy` (export/get-only) — the pub must be the SEC1-encoded `pub` octet string, so imported EC keys have **no public point** and verify dies at `ossl_ecdsa_simple_verify_sig` ecdsa_ossl.c:457 `EC_R_MISSING_PARAMETERS`, the exact observed error. Blast radius bigger than filed: jose RS256 (scrambled RSA import → sign "succeeds" but verify gets `invalid padding`) and all ECDH-ES legs (imported peer epk) were red too, plus gencrypto; fails closed everywhere, no bypass. Affects every openssl 3.x since 4e59faf9d (1.x/2.x legacy branch fine), not just 3.5.7. Fixed by rebuilding both imports on `OSSL_PARAM_BLD` (EC: group + SEC1 `0x04‖X‖Y` pub + push_BN priv, X/Y presence+length gated; RSA: push_BN e/n/d/rsa-factor1/rsa-factor2) — malformed X/Y now rejected at import instead of at verify; misleading shared verify log label made branch-correct (`EVP_PKEY_verify fail`); the modern-branch `ctx->bn[]` pre-conversion that `lws_genrsa_destroy()` never freed moved to the legacy branch (5-BIGNUM-per-import leak gone). Fence: cose/jose/gencrypto red→green on true openssl 3.5.7 plain + ASAN+UBSAN+LSan, full ctest green openssl 141 / gnutls 151 / mbedtls 141, -Werror clean. Note: the F-036 session's "F-039 did not reproduce" was a gnutls-linked build — HTTP3 default forces `LWS_WITH_GNUTLS=ON` (CMakeLists.txt:282); an openssl build needs `-DLWS_WITH_HTTP3=0 -DLWS_WITH_GNUTLS=OFF`, check with ldd) |
| F-040 | Low | high | tls-mbedtls (consumers: jose, cose) | mbedtls v4/PSA genec import never validates X/Y element lengths against the curve or each other (the pre-v4 import in the same file does) — the peer-point build memcpy's Y using X's length: heap OOB read up to ~65B past a short Y allocation on attacker-influenced epk/verification-key elements (JWK import checks presence, not lengths); read bytes only feed the then-malformed point (fail-closed), no exfil path identified | [F-040](findings/F-040.md) | (F-036 fix session) | fixed (231eb1c19 on main, verified SA-053; exactly the recommended shape — the pre-v4 import's `!= curve->key_bytes` triple-check (d-if-present/x/y) added once after curve resolution, covering both the THEIRS peer_key build and the pub-only OURS `pub[133]` staging, existing 133 caps kept as belt-and-braces; en route `ctx->has_private = !!has_private` since the never-compiled v4 TU doesn't build under the project's own `-Wconversion`; fences: api-test-gencrypto `test_genec3` (P-521 x66/y1 worst case, x1/y32, d1 — exact-size heap allocations, clean reject + destroyable ctx) and a third variant in api-test-jose's malformed-epk fence (full-length x + 1-byte y through the real JWK→set_key path); verified by compiling the real lws-genec.c with `LWS_HAVE_MBEDTLS_V4` against 3.6.5's PSA (full-library v4 link still impossible here — mbedtls-x509.c needs v4-only `mbedtls/private/pk_private.h`): ASAN **red** pre-fix heap-buffer-overflow READ 66-from-1B at the second memcpy (and READ 32 from the 8-byte JWK element on the attacker path), **green** post-fix -23 all shapes + exact-length control imports; full ctest 141/141 mbedtls/gnutls/openssl ldd-verified, gencrypto/jose/cose ASAN+UBSAN+LSan clean on openssl+mbedtls; residual: fence legs not yet run on a true v4+TF-PSA-Crypto stack, none available here) |
| F-041 | Low | high | tls-schannel (all genec backends; consumer: auth-dns sign) | every genec backend's `create()` re-inits over a live ctx without freeing the prior incarnation (memset-wipe in gnutls/bearssl/mbedtls incl. PSA key_id; NULL-without-free in openssl/openhitls; new hAlg over old in schannel) and several `new_keypair()`s clobber slots directly — F-036's last-set-wins hardening stops at set_key; in-tree reachable via the auth-dns sign.c DNSKEY loop (create() per RR on shared ctxs, DNSKEY RRsets normally carry 2+ same-flag keys); openssl create() additionally leaves stale has_private over NULL slots (post-recreate sign = NULL deref) | [F-041](findings/F-041.md) | (F-036 fix session) | fixed (80d14be55 on main, verified SA-056; backend-side destroy-if-alive via a new `created_mark` in the public `struct lws_genec_ctx` — create() is the one genec api that has always tolerated uninitialized caller storage, so it cannot blindly free slot contents; the mark distinguishes a ctx that may hold live backend keys, `lws_genec_destroy()` clears it. All six backends' EC creates + openssl/openhitls/schannel EdDSA creates destroy-if-marked → memset → init → mark-on-success (memset adopted from the siblings closes the openssl stale-`has_private`/NULL-slot sign hazard); `new_keypair()` slot-release parity beyond the F-036 gnutls guard — openssl both branches + EdDSA, openhitls (+ NULL after the pre-existing free, was dangling on failed NewCtx), bearssl kbufs/pub.q, schannel hKey, mbedtls pre-v4 bail paths now full `lws_genec_destroy()` (plain `lws_free` was leaking the ecdh/ecdsa MPI state); sign.c DNSKEY loop destroys the previous zsk/ksk jwk before ownership transfer. Fences: api-test-gencrypto `test_genec4` (re-create over live two-sided ECDH proven == fresh-ctx derive, ECDSA re-create sign, new_keypair-on-live-ctx) + api-test-auth-dns zone gains a second ZSK-class DNSKEY so the round-trip displaces a live ctx incarnation; red/green — gnutls natural-red 837B/12allocs (auth-dns) + test_genec4 pub-handle leaks, openssl A/B-red 6325B/130allocs, mbedtls A/B-red 872B/13allocs, all green with gencrypto/auth-dns/jose/cose zero-leak on gnutls 3.8.9 + openssl 3.5.7 + mbedtls 3.6.5 ASAN+UBSAN+LSan; full ctest 142/142 openssl, 155/155 gnutls, 142/142 mbedtls plain (AUTH_DNS on — first time, flushing out a missing `LWS_WITH_SYS_ASYNC_DNS` gate in api-test-dns-server); v4 TU compiled with `LWS_HAVE_MBEDTLS_V4` against 3.6.5 PSA `-Werror`-clean; bearssl/openhitls/schannel code-read only (unbuildable here, mirror their own destroy idioms); en route filed+fixed **F-042** (NSEC3 chain wrap UAF + whole-zone leak), **F-043** (gnutls set_key length validation + the fence's `>= 0` convention bug), **F-044** (`lws_jws_sig_confirm` jose never destroyed — embedded-jwk headers leak per verify), and filed **F-045** open (genrsa create() same class)) |
| F-042 | Low | high | system-auth-dns | NSEC3 chain builder frees each node inside the loop while later iterations' `next_idx` wraps to 0 and reads `nodes[0]->b32` — heap-use-after-free on every zone with 2+ hashed names (every `lws_auth_dns_sign_zone` run, invisible without ASAN); signer additionally never frees the parsed zone at all, and unhashable names leak their node | [F-042](findings/F-042.md) | (F-041 fix session) | fixed (80d14be55 on main, verified SA-059; node frees deferred to a post-chain loop (covers unhashable names too), `lws_auth_dns_sign_zone` frees the zone at the post-parse exits; api-test-auth-dns under ASAN red heap-UAF READ sign.c:1032 freed-at-1060 / green zero-leak round-trip on 3 backends) |
| F-043 | Low | high | tls-gnutls | gnutls genec `set_key` pads X/Y/D of any length into the curve size instead of rejecting them — F-040's length-validation contract existed only on the other backends; masked because the F-040 fence treated `set_key() >= 0` as acceptance while gnutls signals failure with `+1` (nonzero-fails convention) | [F-043](findings/F-043.md) | (F-041 fix session) | fixed (F-041 fix session, uncommitted; openssl/mbedtls triple-check (`!= curve->key_bytes`, d optional) added after curve resolution, fence comparison corrected to `!set_key(...)`; test_genec3 case 0 red-accepts padded P-521 y=1 pre-fix / rejects post-fix, jose/cose/auth-dns suites green on gnutls — nothing curve-exact is newly rejected) |
| F-044 | Low | high | jose (consumer: auth-dns outer JWS; any embedded-jwk JWT verify) | `lws_jws_sig_confirm` never destroys its local jose — ~20 return sites after `lws_jws_parse_jose`, none cleaned; headers embedding a `jwk` member (auth-dns's outer JWS emits one) leak the parsed header key elements per verification (135B/4 allocs observed); plain alg/kid headers parse to no heap, which is why api-test-jose stayed LSan-clean | [F-044](findings/F-044.md) | (F-041 fix session) | fixed (F-041 fix session, uncommitted; single-exit `goto bail` refactor with `lws_jose_destroy(&jose)` on every path, no behavior change; api-test-auth-dns outer-JWS leg LSan red→green, full api-test-jose green on 3 backends) |
| F-045 | Low | high | tls-schannel (all genrsa backends) | every genrsa backend's `create()` shares the F-041 re-init clobber class (memset/handle-NULL over a live ctx without freeing the prior key incarnation) — surveyed during the F-041 fix; no in-tree re-create-on-live-ctx caller exists (all sites are create→use→destroy), so API-contract hygiene rather than a reachable leak today | [F-045](findings/F-045.md) | (F-041 fix session) / [SA-059](reports/SA-059-genrsa-f045-fix.md) | fixed (379976076 on main, verified SA-059; F-041 marker structure ported to `struct lws_genrsa_ctx` — `created_mark`/`LWS_GENRSA_CTX_CREATED_MARK` "LWGR", create()/new_keypair() destroy-if-marked → memset → init → mark-on-success-only (a failed create must leave no live handles or a later create() wipes them), destroy() clears the mark; all six backends incl. schannel memset adoption + defensive `hAlg` NULL on failed provider open and bearssl create() memset over live kbufs. Same-family fail-path hygiene in the same pass: mbedtls pre-v4 invalid-mode return leaked the fresh heap ctx (check hoisted above alloc), mbedtls-v4 new_keypair leaked the generated PSA key_id on export/parse fail, gnutls create/new_keypair fail paths left live or deinit()ed-dangling key pointers. In-tree callers re-surveyed: all create→use→destroy (jose jwk/jws/jwe, cose key/sign/validate, auth-dns sign.c, async-dns dnssec.c, sshd plugin) so defense-in-depth as filed. Fence: api-test-gencrypto `test_genrsa_recreate` — live ctx proven to operate with key0, re-created over the live incarnation with key1 must roundtrip via pub1 and NOT pub0, second re-create restores key0, new_keypair-on-live-ctx displaces; openssl ASAN A/B red 14134B/113allocs guards-off / zero-leak green; full ctest 142/142 openssl + 155/155 gnutls + 142/142 mbedtls plain; gnutls+mbedtls ASAN four crypto suites zero-leak; v4 TU `LWS_HAVE_MBEDTLS_V4` `-Werror`-clean; bearssl/openhitls/schannel code-read only, unbuildable on this box) |
| F-046 | Medium | high | tls-mbedtls | AUTHORITY_KEY_ID_ISSUER cert-info loop checks each AKID issuer entry against the FULL buffer length but copies at the accumulated `ns.len` offset — ≥2 entries sized near len/2 write past `buf->ns.name` with peer-cert DER via the public `lws_tls_peer_cert_info` api; openssl/openhitls siblings do cumulative accounting, gnutls is unimplemented — mbedtls-only | [F-046](findings/F-046.md) | [SA-065](reports/SA-065-tls-mbedtls-1.md), [SA-069](reports/SA-069-f046-fix.md) | fixed |
| F-047 | Low | medium | tls-schannel | `lws_tls_client_confirm_peer_cert` passes a NULL `pwszServerName` to CertVerifyCertificateChainPolicy when MultiByteToWideChar fails on the stored hostname — the HTTPS policy **skips name matching** for NULL, so any chain-valid cert passes; reachable via the URI fence's deliberate ≥0x80 allowance (invalid-UTF-8 hostnames not rejected) + redirect/DNS control; schannel-only, gated | [F-047](findings/F-047.md) | [SA-073](reports/SA-073-tls-schannel-1.md) | fixed (94cfee782 on main, pre-rebase twin 2a6f55a50, verified SA-075; fail-closed bail with frees on the goto path) |
| F-048 | Low | high | plugin-auth-server | Set-Cookie still composed with lws_snprintf — the 256-byte logout/clearing buffers (lines 2682-2710, 2907+) truncate `Expires/Max-Age=0/HttpOnly/SameSite/Secure` with a long cookie_domain[128]; mint sites use 4096 bufs and are safe in practice; the class F-022 removed from oauth2/login, explicitly left for this unit's first audit | [F-048](findings/F-048.md) | [SA-078](reports/SA-078-auth-server-1.md) | fixed (uncommitted: all composition sites via fail-closed lws_http_cookie_compose, clearing paths in new AUTH_SERVER_CLEAR_COOKIE_SZ buffers, csrf sidecar session-cookie documented exception; fenced by api-test-http-cookie, 136/136 ctest) |
| F-049 | Low | high | plugin-auth-server | admin.js renders client `name`/`redirect_uris` and grants keys into innerHTML and `data-*` attributes unescaped — stored XSS between `*`-grant co-admins (ws properly gated; `u.user` safe by registration charset); F-021 escaper class | [F-049](findings/F-049.md) | [SA-078](reports/SA-078-auth-server-1.md) | fixed (uncommitted) |
| F-050 | Medium | high | plugin-auth-server | auth.js interpolates the `service_name` **urlarg** into innerHTML unescaped in the lacks-grant status box — reflected XSS on the auth origin: core URI fence allow VCHAR (`<>"'`), server sets lacks_grant for any ungranted/attacker-invented service name → isDenied render; unauth link + victim click + active session | [F-050](findings/F-050.md) | [SA-079](reports/SA-079-auth-server-2.md) | fixed (uncommitted: auth.js defines lwsAuthEsc() (F-021/F-049 pattern incl. quote-escaping) and routes every innerHTML-composed dynamic string through it — the reflected serviceName urlarg plus defense-in-depth on data.email, grants keys/levels, log IPs/times, errData.error; fenced by new api-test-auth-js asset-bytes fence, 132/132 ctest) |
| F-051 | High | high | plugin-dnssec-dht-a (+ lib/misc/dht) | DHT RPC wire hash token has NO charset validation in `lws_dht_msg_parse` (length cap only) — GET composes storage paths from it (`%s/%s` + `%.2s/%.2s/%s`) and reads 1KB at any offset (arbitrary file read, chunked); PUT opens `%s/tmp/%s.%08X` O_CREAT\|O_TRUNC and writes later chunks into it (arbitrary create/truncate/write); both run BEFORE/independently of the DNSSEC validation that is the plugin's trust boundary | [F-051](findings/F-051.md) | [SA-083](reports/SA-083-dnssec-dht-1.md) | fixed (0418184f5, verified SA-084: lws_dht_msg_parse rejects any hash token that is not 2-128 lowercase hex chars, gating every consumer at the parser; dnssec plugin adds dht_dnssec_hash_token_ok() defense-in-depth refusals in PUT/GET/RSP handlers before any path composition, plus a dispatch-level charset walk over msg->hash; committed shape verified — sizes consistent with hash[129], all three layers fail-closed; **api-test-dht-msg-parse dir still untracked in git** (commit 0418184f5 added only its README line) — needs git add + CMake wiring for the CI fence, same class as the fops-zip note) |
| F-052 | Medium | high | plugin-dnssec-dht-a (+ lib/misc/dht) | DHT-layer NOTIFY (bencode path, **no auth** — dht-bencode.c:1128) carries a raw `newer_domain` wire string that `do_subscribe_zone`/inline-sub store **unvalidated** in `sub->domain`; it later composes filesystem paths: bootstrap `%s/%s.zone` → `req->cache_dir` → post-validation `mkdir(0770)` + `O_CREAT\|O_TRUNC` write **outside the storage root** (boot-window race, validated-zone content); `do_fetch_zone`'s `dht-hash-` branch uses the remainder **verbatim** for `lws_dir` (existence oracle at storage/../..); plus unbounded subscribed_domains growth from unique attacker domains | [F-052](findings/F-052.md) | [SA-084](reports/SA-084-dnssec-dht-2.md) | fixed (cb5533699, verified SA-085 in-tree / committed shape re-verified SA-087: new lws_dht_valid_domain_name() library gate (1-253 chars, [A-Za-z0-9._-], labels 1-63, no empty labels) documented in lws-dht.h and fenced in api-test-dht-msg-parse; cb_dht NOTIFY intake rejects malformed newer_domain before any storage; do_subscribe_zone validates + stores the canonical cleaned domain (covers ops callers and bootstrap cache_dir composition); do_fetch_zone dht-hash- branch hash-gated before lws_dir/target_hash; 154/154 ctest in the DHT+auth-dns+plugins build; **api-test-dht-msg-parse dir still untracked in git** — fence not in CI until added) |
| F-053 | Low | high | plugin-dnssec-dht-b | dnssec monitor composes IPC JSON responses (get_domains/get_all_tls/get_tls: d_name, raw whois.json/dns_state.json content, domain echoes) and ACME config files (create_tls, set_acme_config: email/org/country/state/locality/profile/directory-url) with **unescaped** strings — the central gate allows `"` in domain/subdomain and update_whois plants arbitrary JSON in whois.json, so an authenticated domain-admin injects arbitrary JSON members into responses consumed by the UI and into configs parsed by the ACME client (crossing the IPC schema boundary); get_zone/get_acme_log/issuer-sanitization in the same file show the correct escaper | [F-053](findings/F-053.md) | [SA-086](reports/SA-086-dnssec-dht-b-1.md) | fixed (6eaf73467, committed shape verified SA-088; central gate rejects domain/subdomain failing lws_dht_valid_domain_name() so quote-bearing names cannot reach any handler; all response-JSON string interpolations (get_domains d_name/local_ds, get_tls + get_all_tls domain/fqdn echoes) routed through lws_json_purify() via a json_escape() helper; create_tls/set_acme_config free-text fields written escaped; raw whois/dns_state/acme_config file embeds now lejp-parse-gated (json_snippet_valid) or `{}` so a corrupt file cannot break the response envelope; **stage 2 (uncommitted in-tree 2026-08-31) — whois treated as fully hostile**: new lws_whois_json_purify() library API (lib/system/whois/whois-json.c under LWS_WITH_SYS_WHOIS, plugin now requires that option) canonicalizes untrusted whois JSON to a fixed whitelist (integer dates ≤2^48, ≤16 nameservers of ≤253 hostname-charset chars, dnssec ≤64/ds_data ≤512 printable, ≤4 KiB canonical output, LEJPCB_COMPLETE required so truncated input is refused, dropped-member `problems` out-param, LEJP_FLAG_FEAT_LEADING_WC needed for first-array-element path matching); update_whois now strict-rejects non-conformant payloads with the stored file untouched and writes only the canonical form, with proper ok/error responses; get_domains embeds only the canonicalized form (4096 buffer, was silent-truncating 2048) covering on-disk tampering and legacy dirty files; monitor.js nameservers innerHTML sink escaped via new escapeHtml() helper; IPC "zone" accumulator capped at 128 KiB (was unbounded realloc-doubling); fenced in new api-test-whois-purify (14 cases) alongside the earlier api-test-dht-msg-parse fences; 155/155 ctest in the DHT+auth-dns+plugins+minimal-examples+whois build; no monitor UDS harness in-tree so handler behavior is not directly IPC-fenced) |
| F-054 | Medium | high | plugin-dnssec-dht-b | auth-dns pending DNSBL query lifetimes: the 5s `dnsbl_timeout_cb` frees the query **without cancelling the async-DNS lookups that hold it as opaque** — a resolver response slower than 5s (routine for blackholed DNSBL servers; attacker-inducible) runs `dnsbl_query_cb` on freed heap (read+write of pending_lookups/vhd/wsi); additionally the `pending_dnsbl` list lacks the RAW_CLOSE wsi sweep that `pending_queries` has, so a TCP client disconnect leaves `q->wsi` dangling and the completion path calls into a destroyed wsi; unauthenticated trigger (one query against a served zone, dnsbl pvo configured) | [F-054](findings/F-054.md) | [SA-088](reports/SA-088-dnssec-dht-b-3.md) | fixed (a6ce5d45f, committed shape verified SA-093: new public `lws_async_dns_cancel_by_opaque(cx, opaque)` destroys ongoing standalone queries by callback-opaque without calling back (piggybacking wsis keep the query but the standalone cb/opaque are detached); all three plugin free sites (5s timeout, cb final-completion, PROTOCOL_DESTROY) cancel first; `lws_async_dns_complete()` now holds `q->completing` across the standalone cb as well (was cleared just before invoking it — a cancel from inside the cb would have destroyed the completing query under complete()'s caller); RAW_CLOSE sweep added for `pending_dnsbl` mirroring `pending_queries`; call-site dead `==LADNS_RET_FAILED` pending_lookups re-decrement removed (cb already accounts synchronously) and NULL-`ads` no longer reaches `dnsbl_cache_add()` on the sync-failure path; fenced in new api-test-auth-dns-dnsbl (resolver pinned to a local UDP stub that answers at t+7s, after both 5s timeouts; UDP late-reply leg + TCP disconnect leg) — ASAN A/B red on both filed stacks (`dnsbl_query_cb` read on q freed by `dnsbl_timeout_cb` via `lws_adns_parse_udp`, and `lws_get_protocol` from `dnsbl_timeout_cb` on the freed client wsi) / green fixed; 146/146 ctest auth-dns+DNSSEC+IPv6 build, fence also green IPv4-only/no-DNSSEC and no-SMD dimensions) |
| F-055 | Medium | high | plugin-dnssec-dht-b | auth-dns defaults its zone dir to **world-shared /tmp/lws-auth-dns** and the init dir-scan loads every `*.zone` with syntax-parse only — no DNSSEC verification, no ownership/perms check — so any local user can pre-create/own the dir and inject authoritative zones (arbitrary records served for their origin), and the update/evict paths `unlink` by origin-prefix match (targeted deletion of the operator's zone files); the DHT path is fenced by the DNSSEC chain, the local-dir path has no equivalent | [F-055](findings/F-055.md) | [SA-088](reports/SA-088-dnssec-dht-b-3.md) | fixed (4f06c2fff, committed shape verified SA-094: the /tmp/lws-auth-dns default is deleted — PROTOCOL_INIT fails closed without the "zone-dir" pvo, before dht register/sul/listeners so the freed vhd cannot be held anywhere; at init the zone dir must exist as a real O_NOFOLLOW-opened dir owned by the effective uid with no group/world write, else fail closed; the dir-scan only admits the decorated `<origin>_<ttl>_<sig>_<serial>.zone` shape via a strict forward parser with origin validated presentation-format **without underscores** (underscore origins would make the origin-prefix unlink matching ambiguous — the spray-a-victim-prefix leg), files opened O_NOFOLLOW and fstat-required regular + euid-owned + not group/world-writable; the DHT install path gates its own minted filename label with the same origin check; RAW_RX bails on NULL vhd since refused-init vhosts still have TCP listeners bound — verified clean close; in-tree fixture consumers migrated (dns-server + dnsbl zone files renamed to decorated 2033-expiry shape, modes normalized, test mains chmod-normalize against umask-002 checkouts); fenced in new api-test-auth-dns-zonedir, 7 phases incl. root-only foreign-uid file/dir legs, refusal asserted by silence + UDP-port-bindable; A/B: pre-fix the 0777-dir planted zone is served rcode 0/anc 1 exactly as filed, post-fix PASS incl. root; 147/147 ctest no-DHT build, auth-dns tests green in DHT+ASAN dimension) |
| F-056 | High | high | plugin-ssh | sshd USERAUTH signature-blob walk advances by an attacker-chosen inner u32 length with **no bound against `ua->sig_len`** (`pp += m; m = lws_g32(&pp)` then genrsa imports an m-byte MPI from the wild pointer) — **pre-auth remote heap OOB read** reachable by presenting the authorized user's genuine public blob (public info; passes is_pubkey_authorized) plus a malformed signature string; the pubkey-blob walk (ssh-rsa e/n) has the same unbounded shape, fenced in the demo only by the consumer's byte-compare callback; secondary: empty `exec` string reads `last_alloc[1..3]` past a 1-byte alloc (post-auth) | [F-056](findings/F-056.md) | [SA-089](reports/SA-089-ssh-1.md) | fixed (28fb5b12c, committed shape verified SA-095: sshd.c walks the USERAUTH pubkey and sig blobs through new ssh_blob_item_get()/ssh_parse_pubkey_blob()/ssh_parse_sig_blob() — every inner u32 length must fit inside the remaining blob and the items must exactly consume it, both parses before lws_genrsa_create() so no failure path leaks the RSA ctx; the "scp " exec prefix check is now npos-bounded memcmp; fenced in new api-test-sshd-userauth — in-process sshd vhost + raw client, no KEX (plaintext-transport USERAUTH, all-zero pre-KEX session_id signed with a runtime-generated RSA keypair), 7 phases: lax-ops malformed pubkey blob (huge inner len / off-by-one), genuine-blob malformed-sig legs (huge inner algo len / sig len +1 past blob / trailing junk), genuine-signature success + session channel + 1-byte exec → CHANNEL_FAILURE; A/B red SIGSEGV (exit 139) with unbounded walks restored / green fixed incl. ASAN build; **correction**: the secondary empty-exec last_alloc[1..3] "overread" is unreachable as filed — the \|\| chain stops at the first mismatching compare and the alloc is NUL-terminated at [len], so bare-index reads were always in bounds (length-explicit form kept as hardening, no behavior change)) |
| F-057 | Low | high | ss-serialized | sspc tx composes the initial STREAMTYPE frame with a length prefix from the **uncapped** `strlen(streamtype)` but copies into a 64-byte stack scratch with silent `lws_strncpy` truncation — an app-supplied streamtype of 52+ chars makes the client `_write()` past the scratch onto the proxy UDS (local stack-content disclosure + serialization desync); `lws_sspc_create` applies no length cap, and the proxy parser's own `streamtype[32]` rejects ≥31 anyway, so only application misuse reaches the over-read | [F-057](findings/F-057.md) | [SA-100](reports/SA-100-ss-serialized-3.md) | fixed (22cf5b9bc, committed shape: new public `LWS_SS_SER_STREAMTYPE_MAX_LEN` (31) defined in lws-secure-streams-client.h and used to size the shared `lws_ss_serialization_parser.streamtype[]` so the proxy parser buffer follows the published contract; `lws_sspc_create()` now refuses streamtypes over the cap up-front with a loud `lwsl_cx_err` before any allocation or transport deref, closing every path incl. the initial-tx composition; defense-in-depth in `sspc-transport.c` SENDING_INITIAL_TX: if the streamtype cannot fit the 64-byte scratch behind the 12-byte header it fails the link (hangup) instead of truncating, so the frame length prefix can never claim more than was composed; fenced in new api-test-sspc-streamtype — no live proxy needed since the cap fires at create: 32-char and 60-char (OOB-range) streamtypes asserted refused, 31-char boundary asserted accepted then destroyed cleanly; A/B red with the create-cap disabled (both refusal legs fail, boundary passes) / green fixed incl. ASAN build; 77/77 SS-related ctest green in the proxy-api build) |
| F-058 | Low | high | roles-mqtt | `lws_mqtt_client_send_unsubcribe()` indexes a fixed `send_unsub[8]` by the caller-supplied `num_topics` with **no bound check** — an app passing >8 topics gets a **stack OOB write** (plus OOB reads in the composition loops); the sibling subscribe function asserts `num_topics < sizeof(exists)` but the unsubscribe entry lacks the equivalent guard; no wire input reaches num_topics and in-tree callers pass 1 and 2, so app-contract only (F-057 precedent) | [F-058](findings/F-058.md) | [SA-107](reports/SA-107-mqtt-2.md) | fixed (d216c31ba, committed shape verified SA-111: new public `LWS_MQTT_MAX_TOPICS` (8) in lws-mqtt.h publishing the num_topics contract, both `exists[]`/`send_unsub[]` scratch sized by it, and both subscribe and unsubscribe entries now refuse zero or over-cap `num_topics` up-front with a loud `lwsl_err` + nonzero return before any refcount/scratch/composition — the sibling's NDEBUG-vanishing asserts subsumed; fenced in new api-test-mqtt-unsub — in-process fake MQTT broker (ONLY_RAW vhost, CONNACK/SUBACK/UNSUBACK) + real lws mqtt client over loopback so the refusals fire inside the actual ESTABLISHED-state tx paths: 9-topic unsubscribe with a genuinely-subscribed first topic, 0-topic unsubscribe and 9-topic subscribe all refused with exactly one loud rejection each and no side effects, boundary 8-topic unsubscribe completes a real UNSUBSCRIBE/UNSUBACK end-to-end; A/B red with guards disabled (all refusal legs fail in the normal build, and the 9-topic leg SIGILLs at the OOB composition loop under the ASAN build's ubsan-trap) / green fixed incl. 5/5 clean ASAN runs; 139/139 ctest green in a fresh LWS_ROLE_MQTT build (the ASAN build's 16 unrelated failures — qpack, http-client-multi etc — reproduce identically from pristine HEAD); incidental core fix in the same patch for a pre-existing teardown UAF the fence exposed: the CONNACK-created "nwsi duties" wsi stayed on the pt pre-natal list and `rops_close_kill_connection_mqtt()` never closed mux children of the non-substream connection wsi, orphaning it with a dangling `mux.parent_wsi` that `lws_quic_stream_cleanup()` walked on freed memory at context destroy — now removed from pre-natal at identity takeover and the close-children gate covers any wsi still holding mux children, h2-equivalent style) |
| F-059 | Medium | high | plugin-hls | `lws_hls_serve_dir()` composes the listing HTML with the naive `p_html += snprintf(p_html, rem, ...)` pattern over a fixed 512-per-entry estimate while interpolating `name[256]` **four times** per entry — long filenames truncate, the cursor overshoots the allocation, the next `rem = html_size - used` **underflows to a huge size_t**, and the following snprintf writes unbounded past the heap buffer (**heap OOB write**; the exact class hls-sub.c's append_fmt documents and lws_snprintf clamps — this file uses raw snprintf); the same names interpolate into HTML text and the single-quoted `data-file` attribute **unescaped** (stored XSS on the HLS origin, F-021 family) | [F-059](findings/F-059.md) | [SA-111](reports/SA-111-hls-2.md) | fixed (b839a0388, committed shape verified SA-114: new `hls_dir_esc()` two-pass HTML escaper (& < > " ' → entities, exact-size via first cap=0 pass, &#39; worst-case 255×5+1 sizing) applied to all four name interpolations incl. data-file; the clamped-cursor `append_fmt` hoisted to shared `hls_append_fmt()` in private-lws-hls.h and **all** composition routed through it — accounting errors can only truncate now; buffer sized exactly = chrome + Σ(4×escaped-name-len + fixed-markup), killing the 512-per-entry underestimate; lws_snprintf for the path; realloc-to-temp for entries) |
| F-060 | Low | high | system-subdirs | ota.c:483 the manifest variant anti-brick gate `strncmp(lws_ota_variant, p, alen)` uses the **manifest value's length** — a signed variant that is a strict prefix of the build's `LWS_OTA_VARIANT` ("myproduct" vs "myproduct2") compares equal, so a validly-signed wrong-variant manifest passes the gate built specifically to refuse it (wrong-firmware flash → brick); same length-blind shape on the `"alg":"ES512"` check at :464 but inert there (sig_confirm already resolved a verifiable alg, none a strict prefix) — F-022 class | [F-060](findings/F-060.md) | [SA-131](reports/SA-131-system-subdirs-2.md) | fixed (2a77588e5; variant check now requires alen == strlen(lws_ota_variant) + memcmp; alg check likewise exact-length); committed-shape verified SA-132 (HEAD-line content-twin 4ed29dd84) |
| F-061 | Low | high | system-subdirs | ota.c `update_impossible` (ota_start failure after inflator+genhash exist) sets `state = LWSOS_FAILED`, which sits **after** `LWSOS_FINALIZING` in the enum (private-lib-core-net.h:1221), so both DESTROYING cleanup gates (`== FETCHING_INITED_GZ_HASH`, `< FINALIZING`) miss it — the upng inflator (zlib state, tens of KB) + SHA512 genhash ctx leak at SS destroy, once per failed attempt, unbounded over the automatic 24h/periodic retries on devices with persistently failing `ota_start` → slow OOM DoS on embedded targets | [F-061](findings/F-061.md) | [SA-131](reports/SA-131-system-subdirs-2.md) | fixed (7fdf4723a; DESTROYING frees by ownership: new hash_inited flag for the genhash ctx, g->inflate pointer for the inflator (also removes the double lws_upng_inflator_destroy() after the write-completion path destroyed both while state stayed in the FETCHING range)); committed-shape verified SA-132 (HEAD-line content-twin b83219c9c) |
| F-062 | Low | high | system-subdirs | ota.c:500-501 `lws_strnncpy(g->file, p, alen, sizeof(g->file))` truncates a long manifest `"path"` into `file[128]` but the **original unbounded `alen`** is then passed to `lws_ss_set_metadata(..., "file", g->file, alen)` → heap OOB read of up to ~1.9KB past `g->file` into the SS metadata value (rest of lws_ota_t + adjacent heap, interpolated into the onward fetch URL); reachable only by a valid ES512 manifest signer who already holds flash authority, so no boundary crossing today — filed Low as a real ASAN-grade overread with a one-line fix | [F-062](findings/F-062.md) | [SA-131](reports/SA-131-system-subdirs-2.md) | fixed (4cbbe96ee; path with alen >= sizeof(g->file) is rejected before the copy, so the metadata length always describes bytes g->file holds; committed-shape verified SA-132, incl. pre-rebase twin 64932d151) |
| F-063 | High | high | roles-http | client digest auth `lws_http_digest_auth()`: `response[LWS_GENHASH_LARGEST]` (=64, client-http.c:637) is written by `lws_hex_from_byte_array(..., lws_genhash_size(hash_type) * 2 + 1)` (:945-948) where **hash_type is selected by the server's `algorithm=` challenge** — SHA-512 writes 129 bytes into the 64-byte stack buffer (**65-byte pre-auth stack OOB write**, default build, one 401 round trip; 33 bytes SHA-384, 1 byte SHA-256); the C-001 commit `379079589` *describes* the resize ("Size it like the others") but its diff shipped only the Authorization-line bound — the overflow is still live; SA-040 miss acknowledged | [F-063](findings/F-063.md) | [SA-132](reports/SA-132-coverity-core-protocols.md) | fixed (90f3a6d7f; `response[LWS_GENHASH_LARGEST * 2 + 1]` at client-http.c:637 matching the a1/a2/digest siblings — the resize half of C-001 whose diff never landed; `lws_hex_from_byte_array()` bounds by the passed len (129 for SHA-512) not the dest size, so the buffer must hold the hex form; verified by trace re-derivation + ASan `build-zsa` `-Werror` clean, full ctest 137/141 with only 4 pre-existing libgnutls-internal LeakSanitizer failures identical without the change); committed-shape verified SA-135 (90f3a6d7f in the HEAD line, exactly the prescribed resize) |
| F-064 | Low | high | misc-svg-renderer | svg.c:1463 shape commit stores the composed-CTM point with no range check — nested `scale(1e9)` groups (depth cap 24) push the double product past float max, so scene points become **±inf** (and a later 0×inf compose gives NaN in m[] → NaN points); at raster an inf-y edge crossing a finite neighbour yields `t = -inf/-inf = NaN` stored in `xings[]` → `qsort` runs with a **non-strict-weak-order comparator** (NaN compares 0 both ways, documented UB) and `iceil()` casts non-finite doubles to int (**float-cast-overflow UB**); both degrade to wrong/no output on mainstream platforms, but violate the file's "hostile-input-safe" contract and trip sanitizers — fix: finiteness/|1e15| gate at commit | [F-064](findings/F-064.md) | [SA-134](reports/SA-134-svg-renderer.md) | fixed (860765473 "svg: pure integer arithmetic end to end" — structural fix stronger than the gate: no doubles remain anywhere in the geometry, coordinates are saturating Q16.16 int32 via svg_qadd/svg_qmul (int64 intermediates clamped to ±SVG_C_MAX), number parser builds e8 ints clamped to ±1e6, cross_cmp orders int32s (consistent by construction), no (int) casts of non-finite doubles exist — the filed UB class is unreachable; committed-shape verified SA-135) |
| F-065 | Low | high | misc-svg-renderer | svg.c:1716 the strokes miter-join test `mlq * mlq * cos2 >= 2^48` **overflows int64 for legal-range `stroke-miterlimit` values** — `miterlimit` is floored at 1 (:2552) but never ceilinged and the parse cap is 1e6 whole units, so the product passes int64 max at miterlimit ≈ 180 w/ a near-straight join (≈1000 overflows for most angles; at the 1e6 cap `mlq²` overflows unconditionally) — signed-overflow UB on hostile input: SIGILL under the project's `-fsanitize-undefined-trap-on-error` builds (dcac286eb precedent), arbitrary miter/bevel decision otherwise; downstream geometry all saturates (no memory-safety impact) — fix: ceiling miterlimit at apply time (255/1e3) or overflow-free comparison | [F-065](findings/F-065.md) | [SA-137](reports/SA-137-svg-strokes.md) | fixed (a57ad988a "svg: F-065: make the miter-limit test overflow-free and ceiling miterlimit" — both recommended layers applied: the threshold is formed by two exact ceil-divisions `ceil(2^48/mlq/mlq)` so no product exists for any miterlimit, and miterlimit is ceilinged at 128 whole units at level_compose() next to the spec floor (any limit >= 32 mitters every join that survives the near-reversal cutoff, so no decision changes); api-test-svg regression fence stroke-ml-huge (1e6) + stroke-ml-straight/-200; UBSan verified both directions: pre-fix corpus aborts at :1719 `65536000000 * 65536000000`, post-fix clean); committed-shape independently verified SA-139 (ceil-division comparison + 128-unit ceiling both as recommended, in the HEAD line) |
| F-066 | Low | high | lhp-dlo-ui | `lws_fx_mul` computes the whole-part product `w = a->whole * b->whole` in **int32×int32** (libwebsockets.c:2562) — lhp's CSS intake cap (LHP_CSS_MAX_WHOLE = 1e6) keeps single values in int32 but two legal-range operands ≥ ~46342 overflow the product: reachable from hostile CSS via the percent cascade (`lws_css_compute_cascaded_length` multiplies each level's value by the accumulated length, 20 levels) and percent×base/em chains — **signed-overflow UB** (trap class, the F-065/CID-913540 family) + wrapped layout magnitudes; downstream memory-safe (dlo bounds-checks vs the surface, svg saturates) — fix: int64 staging + saturation like svg's svg_qmul, or cap intake at ~46341 | [F-066](findings/F-066.md) | [SA-140](reports/SA-140-lhp-pass2.md) | fixed (e1e10b96b; lws_fx_add/sub/mul stage the whole part in int64 and saturate at +/-INT32_MAX, the frac staging was already int64; api-test-fx fences mul both signs + the 46342.5^2 boundary, add and sub saturation; hostile css width: 900000% of 900000px now lays out at the saturated width, no UB) |
<!-- append new findings above this line -->
