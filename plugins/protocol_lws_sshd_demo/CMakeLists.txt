set(PLUGIN_NAME protocol_lws_sshd_demo)
set(requirements 1)
require_lws_config(LWS_WITH_SSL 1 requirements)

if (requirements)
	create_plugin(${PLUGIN_NAME} "../protocol_lws_ssh_base/include" "protocol_lws_sshd_demo.c" "" "")
	if (NOT LWS_WITH_PLUGINS_BUILTIN)
		target_compile_definitions(${PLUGIN_NAME} PRIVATE LWS_BUILDING_SHARED)
	endif()
endif()
