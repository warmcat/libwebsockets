set(PLUGIN_NAME protocol_lws_quic_server)
set(requirements 1)
require_lws_config(LWS_ROLE_QUIC 1 requirements)

if (requirements)
	create_plugin(${PLUGIN_NAME} "lws-quic-server" "protocol_lws_quic_server.c" "" "")
	if (NOT LWS_WITH_PLUGINS_BUILTIN)
		target_compile_definitions(${PLUGIN_NAME} PRIVATE LWS_BUILDING_SHARED)
	endif()
endif()
